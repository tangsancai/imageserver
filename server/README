	文件说明：
		picvserver.cpp：服务器端的业务逻辑
		serv.h：功能函数库
		serv.cpp：功能函数的具体实现
		
		server_log：服务器运行日志，定时写回硬盘，服务器崩溃排错用
		s：编译后的执行文件
		makefile：makefile

		simgupload：该文件夹用于存储新加入图片库的图片，服务器开启时会提取这些图片的特征，将特征写入特征库，并将该图片移至图片库
		simg：图片库（因文件太大未能上传）
		sxml：特征库（因文件太大未能上传）
		simgtmp：用于临时存储客户端发来的待检索图片
